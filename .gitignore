/target
Cargo.lock

# RISC-V build trees. The fixtures are outside the workspace, so they each get
# their own target dir. Their .elf and .objdump goldens are committed; the
# trees that produce them are not.
/fixtures/*/target

# mdbook output
/docs/book
